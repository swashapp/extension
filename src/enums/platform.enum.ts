export enum PlatformOS {
  WINDOWS = "win",
  MAC = "mac",
  IOS = "ios",
  IPADOS = "ipados",
  LINUX = "linux",
  ANDROID = "android",
  CROS = "cros",
  OPENBSD = "openbsd",
  UNKNOWN = "unknown",
}

export enum PlatformType {
  Desktop = "desktop",
  Mobile = "mobile",
}
